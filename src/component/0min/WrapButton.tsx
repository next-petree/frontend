import styled from "@emotion/styled";

export const WrapButton = styled.button`
  background-color: transparent;
  border: none;
  padding: none;
  margin: none;

  :hover {
    opacity: 0.8;
    cursor: pointer;
  }
`;
