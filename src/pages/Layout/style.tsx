import styled from "styled-components";
import { Container } from "@mui/material";

export const LayoutPageContainer = styled(Container)``;
export const LayoutPageContent = styled.div`
  display: flex;
  flex-direction: column;
  flex: 1;
`;
