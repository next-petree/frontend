import { TestResult } from "@component/Test/TestResult";

/**
 *
 * 점수가 공개될시, 애니메이션 적용하여 점수조회 (정확히 어떤 애니메이션??)
 *
 */

const TestResultPage = () => {
  return <TestResult />;
};
export default TestResultPage;
